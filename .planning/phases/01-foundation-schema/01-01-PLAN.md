---
phase: 01-foundation-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - .env.example
  - .gitignore
  - lib/main.dart
  - lib/app.dart
  - lib/core/constants/app_constants.dart
  - lib/core/constants/supabase_tables.dart
  - lib/core/utils/money.dart
  - lib/core/theme/app_theme.dart
  - lib/core/errors/app_exceptions.dart
  - lib/providers/database_providers.dart
  - lib/routing/app_router.dart
autonomous: true
requirements:
  - PLAT-05

must_haves:
  truths:
    - "Flutter project compiles with zero errors on `flutter analyze`"
    - "All dependencies resolve with `flutter pub get`"
    - "Money value object correctly converts between paise and rupee display strings"
    - "App entry point initializes Supabase (placeholder) and Riverpod ProviderScope"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies declared"
      contains: "supabase_flutter|drift|flutter_riverpod|go_router|freezed_annotation"
    - path: "lib/main.dart"
      provides: "App entry point with Supabase init"
      contains: "Supabase.initialize"
    - path: "lib/app.dart"
      provides: "MaterialApp.router with Riverpod"
      contains: "MaterialApp.router"
    - path: "lib/core/utils/money.dart"
      provides: "Integer paise value object with INR formatting"
      contains: "class Money"
    - path: "lib/routing/app_router.dart"
      provides: "GoRouter stub with placeholder routes"
      contains: "GoRouter"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: MyApp()))"
      pattern: "ProviderScope.*MyApp"
    - from: "lib/app.dart"
      to: "lib/routing/app_router.dart"
      via: "routerConfig property"
      pattern: "routerConfig.*goRouter"
---

<objective>
Create the Flutter project scaffold with all Phase 1 dependencies, core utilities, and app infrastructure that all subsequent plans build upon.

Purpose: Establishes the cross-platform Flutter project (PLAT-05) with the hybrid folder structure (layer + feature), all required packages, and core utility classes that every future module depends on.
Output: A compilable Flutter project with dependency resolution, Money value object for integer paise, Material 3 theme, GoRouter stub, and Riverpod provider skeleton.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md (§1 Flutter Project Setup — folder structure, pubspec.yaml, env configuration)
@.planning/phases/01-foundation-schema/01-CONTEXT.md (locked decisions: integer paise, client-side UUIDs, separate updated_at/synced_at)
@.planning/phases/01-foundation-schema/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter project and install dependencies</name>
  <files>pubspec.yaml, analysis_options.yaml, .gitignore, .env.example</files>
  <action>
    1. Run `flutter create . --project-name personal_finance --org com.household` in the project root directory (existing files like AGENTS.md and LICENSE will be preserved).
    2. Replace pubspec.yaml contents with the exact dependency list from research §1 (supabase_flutter ^2.8.0, drift ^2.22.0, drift_flutter ^0.2.4, sqlite3_flutter_libs ^0.5.28, flutter_riverpod ^2.6.1, riverpod_annotation ^2.6.1, go_router ^14.8.0, freezed_annotation ^2.4.4, json_annotation ^4.9.0, uuid ^4.5.1, intl ^0.19.0, shared_preferences ^2.3.4, flutter_dotenv ^5.2.1, and all dev_dependencies including drift_dev, build_runner, freezed, json_serializable, riverpod_generator, mockito).
    3. Set `environment: sdk: ^3.7.0`.
    4. Run `flutter pub get` to verify all dependencies resolve.
    5. Create `.env.example` with placeholder values:
       ```
       SUPABASE_URL=https://your-project.supabase.co
       SUPABASE_ANON_KEY=your-anon-key
       ```
    6. Add `.env` to `.gitignore` (keep .env.example tracked).
    7. Set `minSdkVersion 21` in `android/app/build.gradle` for Supabase/Drift compatibility.
    8. Update analysis_options.yaml to include recommended flutter_lints rules.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>Flutter project exists with all Phase 1 dependencies resolving, minSdkVersion 21 set, .env.example created, .env gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Create core infrastructure files</name>
  <files>lib/main.dart, lib/app.dart, lib/core/constants/app_constants.dart, lib/core/constants/supabase_tables.dart, lib/core/utils/money.dart, lib/core/theme/app_theme.dart, lib/core/errors/app_exceptions.dart, lib/providers/database_providers.dart, lib/routing/app_router.dart</files>
  <action>
    1. Create the hybrid folder structure per research §1:
       ```
       lib/
       ├── main.dart
       ├── app.dart
       ├── core/
       │   ├── constants/
       │   ├── extensions/
       │   ├── errors/
       │   ├── utils/
       │   └── theme/
       ├── data/
       │   ├── database/
       │   │   ├── tables/
       │   │   └── daos/
       │   ├── models/
       │   └── repositories/
       ├── services/
       │   └── auth/
       ├── providers/
       ├── features/
       │   ├── auth/
       │   │   ├── screens/
       │   │   └── widgets/
       │   └── dashboard/
       │       ├── screens/
       │       └── widgets/
       └── routing/
       ```
       Create placeholder .gitkeep files in empty leaf directories.

    2. **main.dart**: Entry point that loads .env via flutter_dotenv, calls Supabase.initialize() with env vars (use authFlowType: AuthFlowType.pkce), wraps app in ProviderScope, runs MyApp. Include WidgetsFlutterBinding.ensureInitialized(). Use try/catch around Supabase.initialize for graceful failure if env vars missing.

    3. **app.dart**: StatelessWidget MyApp that returns MaterialApp.router using GoRouter from routing/app_router.dart. Apply the Material 3 theme from core/theme. Set debugShowCheckedModeBanner: false.

    4. **core/constants/app_constants.dart**: Constants class with:
       - `paiseMultiplier = 100` (₹1 = 100 paise)
       - `syncIntervalSeconds = 30`
       - `inviteExpiryDays = 7`
       - `maxFileSizeBytes = 5 * 1024 * 1024` (5MB)
       - `defaultCategories = ['Food', 'Transport', 'Shopping', 'Bills', 'Health', 'Entertainment', 'Education', 'Miscellaneous']`

    5. **core/constants/supabase_tables.dart**: String constants for all Supabase table names (profiles, households, household_members, household_invites, wallets, wallet_categories, transactions, etc. — every table from the schema).

    6. **core/utils/money.dart**: Immutable `Money` value class:
       - Constructor from integer paise: `Money(int paise)`
       - Factory `Money.fromRupees(double rupees)` → converts to paise (multiply by 100, round)
       - Getter `int paise` — raw value
       - Getter `double rupees` — paise / 100
       - Method `String format()` — returns Indian number formatting with ₹ symbol (e.g., ₹1,23,456.78 for 12345678 paise). Use `intl` NumberFormat with `#,##,##0.00` pattern and '₹' prefix.
       - Arithmetic operators: +, -, comparisons
       - `toString()` delegates to `format()`
       - Override == and hashCode based on paise value

    7. **core/theme/app_theme.dart**: Material 3 theme with ColorScheme.fromSeed (pick a finance-appropriate seed color like teal/green). Light theme only for now.

    8. **core/errors/app_exceptions.dart**: Simple exception classes: `AuthException`, `DatabaseException`, `SyncException` extending a base `AppException`.

    9. **providers/database_providers.dart**: Stub file with a TODO comment for AppDatabase provider (filled in Plan 03).

    10. **routing/app_router.dart**: GoRouter with stub routes:
        - `/auth/login` → placeholder Container
        - `/auth/signup` → placeholder Container
        - `/onboarding` → placeholder Container
        - `/invite/:token` → placeholder Container
        - `/` → placeholder Center(child: Text('Dashboard'))
        - Add a redirect stub that checks Supabase auth state (session != null) and redirects unauthenticated users to /auth/login. If user is authenticated but has not completed onboarding (check profile.onboarding_completed — for now, just always go to dashboard).
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <done>All core infrastructure files exist, app compiles cleanly, GoRouter redirects based on auth state, Money correctly formats ₹1,23,456.78 from 12345678 paise.</done>
</task>

</tasks>

<verification>
1. `flutter pub get` succeeds with zero errors
2. `flutter analyze --no-fatal-infos` reports no errors
3. Folder structure matches the hybrid layout from research
4. Money(12345678).format() returns "₹1,23,456.78"
5. Money.fromRupees(500.50).paise returns 50050
</verification>

<success_criteria>
- Flutter project compiles and all dependencies resolve
- Core utility classes (Money, theme, constants) are functional
- GoRouter has stub routes with auth redirect guard
- Riverpod ProviderScope wraps the app
- Project structure follows the hybrid layout from research §1
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-01-SUMMARY.md`
</output>

---
phase: 01-foundation-schema
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - lib/data/database/tables/transactions_table.dart
  - lib/data/database/tables/transaction_presets_table.dart
  - lib/data/database/tables/budgets_table.dart
  - lib/data/database/tables/recurring_transactions_table.dart
  - lib/data/database/tables/shopping_tables.dart
  - lib/data/database/tables/investment_tables.dart
  - lib/data/database/tables/documents_table.dart
  - lib/data/database/tables/sms_tables.dart
  - lib/data/database/tables/debt_tables.dart
  - lib/data/database/tables/goals_tables.dart
  - lib/data/database/tables/life_organizer_tables.dart
  - lib/data/database/tables/sync_tables.dart
  - lib/data/database/app_database.dart
autonomous: true
requirements:
  - PLAT-05

must_haves:
  truths:
    - "Drift code generation succeeds with all ~30 tables without errors"
    - "AppDatabase opens and creates all tables (Phase 1 + future-feature) on both Android and Web"
    - "All future-feature Drift table definitions mirror the Supabase schema column-for-column"
    - "All monetary columns use IntColumn (paise), never double"
  artifacts:
    - path: "lib/data/database/tables/transactions_table.dart"
      provides: "Drift Transactions table definition"
      contains: "class Transactions extends Table"
    - path: "lib/data/database/tables/investment_tables.dart"
      provides: "Drift Investment tables (Holdings, Transactions, NavHistory, PriceHistory, SipSchedules)"
      contains: "class InvestmentHoldings extends Table"
    - path: "lib/data/database/tables/sync_tables.dart"
      provides: "Drift SyncQueue table for outbound sync"
      contains: "class SyncQueue extends Table"
    - path: "lib/data/database/app_database.dart"
      provides: "Updated AppDatabase with ALL tables registered"
      contains: "@DriftDatabase.*Transactions"
  key_links:
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/tables/"
      via: "@DriftDatabase tables list includes all future-feature tables"
      pattern: "@DriftDatabase.*tables.*Transactions.*Budgets.*InvestmentHoldings"
---

<objective>
Add all future-feature Drift table definitions (transactions, budgets, investments, documents, shopping, SMS, debt, goals, life organizer, sync) and register them in AppDatabase. This completes the full Drift schema to match Supabase.

Purpose: Completes the offline-first local database schema (PLAT-05) by adding all remaining tables. Tables start empty — future phases populate them. Having the full schema upfront avoids complex Drift migrations later (per user's locked decision: full schema upfront).
Output: ~12 table definition files, updated AppDatabase with all ~30 tables, successful code generation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md (§4 Drift Local Schema — table definitions, AppDatabase, code generation)
@.planning/phases/01-foundation-schema/01-CONTEXT.md (locked decisions: full schema upfront, integer paise, separate updated_at/synced_at, client-side UUIDs)
@.planning/phases/01-foundation-schema/01-03-SUMMARY.md (Phase 1 active tables, AppDatabase with 6 tables, DAOs, provider)
</context>

<interfaces>
<!-- Key contracts from Plan 03 that this plan depends on -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From lib/data/database/app_database.dart (Plan 03):
```dart
@DriftDatabase(
  tables: [Profiles, Households, HouseholdMembers, HouseholdInvites, Wallets, WalletCategories],
  daos: [WalletDao, CategoryDao, HouseholdDao],
)
class AppDatabase extends _$AppDatabase {
  // ... constructor, schemaVersion, migration strategy
}
```

From Plan 03 table naming convention:
```dart
// Each table: class FooBar extends Table { ... @override Set<Column> get primaryKey => {id}; }
// Standard columns: id (text PK), createdAt, updatedAt, syncedAt (nullable), deletedAt (nullable)
// FKs via .references(OtherTable, #column)
// Monetary values: IntColumn (paise)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Drift table definitions for future-feature tables</name>
  <files>lib/data/database/tables/transactions_table.dart, lib/data/database/tables/transaction_presets_table.dart, lib/data/database/tables/budgets_table.dart, lib/data/database/tables/recurring_transactions_table.dart, lib/data/database/tables/shopping_tables.dart, lib/data/database/tables/investment_tables.dart, lib/data/database/tables/documents_table.dart, lib/data/database/tables/sms_tables.dart, lib/data/database/tables/debt_tables.dart, lib/data/database/tables/goals_tables.dart, lib/data/database/tables/life_organizer_tables.dart, lib/data/database/tables/sync_tables.dart</files>
  <action>
    Create Drift table classes mirroring the Supabase schema column-for-column for all future-feature tables. Follow the exact same conventions established in Plan 03 (PKs as TextColumn UUIDs, standard timestamp columns, monetary amounts as IntColumn paise).

    **File organization:**
    - `transactions_table.dart` → Transactions (amount_paise as IntColumn, transfer_ref, split_ref, is_draft, sms_ref)
    - `transaction_presets_table.dart` → TransactionPresets
    - `budgets_table.dart` → Budgets (limit_paise as IntColumn, month_year as TextColumn)
    - `recurring_transactions_table.dart` → RecurringTransactions
    - `shopping_tables.dart` → ShoppingLists + ShoppingListItems (group related)
    - `investment_tables.dart` → InvestmentHoldings + InvestmentTransactions + NavHistory + PriceHistory + SipSchedules
    - `documents_table.dart` → Documents
    - `sms_tables.dart` → SmsRules + SmsMerchantMappings
    - `debt_tables.dart` → DebtInstruments + DebtPayments
    - `goals_tables.dart` → FinancialGoals + GoalContributions
    - `life_organizer_tables.dart` → TaskLists + Tasks + CalendarEvents + Subscriptions + ImportantDates
    - `sync_tables.dart` → SyncQueue (local outbound sync queue: id, tableName, recordId, operation, createdAt — slightly different from Supabase sync_log)

    **Column type mapping (same as Plan 03):**
    - UUID → `TextColumn`, TEXT → `TextColumn`, BIGINT paise → `IntColumn`
    - INTEGER → `IntColumn`, BOOLEAN → `BoolColumn`, TIMESTAMPTZ/DATE → `DateTimeColumn`

    Use `.references(OtherTable, #column)` for FK relationships where practical. Use `.withDefault()` for boolean defaults and sort_order defaults. Use `.nullable()` for optional columns.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos lib/data/database/tables/</automated>
  </verify>
  <done>All 12 future-feature table definition files exist with every column from the Supabase schema mirrored. All monetary columns use IntColumn (paise). All PKs are TextColumn (UUID). No compile errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppDatabase tables list and re-run code generation</name>
  <files>lib/data/database/app_database.dart</files>
  <action>
    1. Update `lib/data/database/app_database.dart`:
       - Add ALL new table classes to the `@DriftDatabase(tables: [...])` annotation
       - Full list should include Plan 03 tables (Profiles, Households, HouseholdMembers, HouseholdInvites, Wallets, WalletCategories) PLUS all new tables from Task 1 (Transactions, TransactionPresets, Budgets, RecurringTransactions, ShoppingLists, ShoppingListItems, InvestmentHoldings, InvestmentTransactions, NavHistory, PriceHistory, SipSchedules, Documents, SmsRules, SmsMerchantMappings, DebtInstruments, DebtPayments, FinancialGoals, GoalContributions, TaskLists, Tasks, CalendarEvents, Subscriptions, ImportantDates, SyncQueue)
       - Add imports for all new table files
       - Keep DAOs list unchanged (WalletDao, CategoryDao, HouseholdDao from Plan 03)
       - Keep schemaVersion at 1 (since this is still initial schema creation, not a migration)

    2. Run `dart run build_runner build --delete-conflicting-outputs` to regenerate:
       - `app_database.g.dart` (now includes all ~30 tables)
       Verify code generation completes without errors.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>AppDatabase annotation includes all ~30 tables. Code generation succeeds. `flutter analyze` reports no errors. Full Drift schema matches complete Supabase schema.</done>
</task>

</tasks>

<verification>
1. `dart run build_runner build --delete-conflicting-outputs` completes without errors
2. `flutter analyze --no-fatal-infos` reports no errors
3. Updated `app_database.g.dart` includes all ~30 table definitions
4. All future-feature Drift tables mirror Supabase schema (same columns, types mapped correctly)
</verification>

<success_criteria>
- All ~30 tables defined in Drift matching the complete Supabase schema
- Code generation produces valid Dart code with all tables
- AppDatabase registers all tables (Phase 1 active + future-feature)
- Schema version remains 1 (initial creation, no migration needed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-04-SUMMARY.md`
</output>

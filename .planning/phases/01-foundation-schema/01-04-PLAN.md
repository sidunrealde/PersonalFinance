---
phase: 01-foundation-schema
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - .env
  - lib/services/auth/auth_service.dart
  - lib/providers/auth_providers.dart
  - lib/providers/wallet_providers.dart
  - lib/features/auth/screens/login_screen.dart
  - lib/features/auth/screens/signup_screen.dart
  - lib/features/auth/screens/onboarding_screen.dart
  - lib/features/auth/screens/invite_accept_screen.dart
  - lib/features/auth/widgets/auth_form_field.dart
  - lib/features/dashboard/screens/dashboard_screen.dart
  - lib/routing/app_router.dart
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - WALL-01

user_setup:
  - service: supabase
    why: "Auth, database, and RLS require a Supabase project"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Settings → API → Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Settings → API → anon/public key"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://supabase.com/dashboard → New Project"
      - task: "Note the project ref (from URL: https://supabase.com/dashboard/project/<ref>)"
        location: "Supabase Dashboard URL"

must_haves:
  truths:
    - "User can sign up with email and password and receive a session"
    - "User can log in with existing credentials and receive a session"
    - "User can log out and be redirected to login screen"
    - "Session persists across app restarts (no re-login needed)"
    - "First user completes onboarding: creates household, names it, gets invite link"
    - "After onboarding, user has a shared wallet and a private wallet with 8 default categories each"
    - "Second user can accept invite via token, auto-gets private wallet + categories"
    - "Unauthenticated users are redirected to login screen"
    - "Flutter app builds and runs on both Android emulator and Chrome"
  artifacts:
    - path: "lib/services/auth/auth_service.dart"
      provides: "Auth operations wrapping Supabase Auth SDK"
      exports: ["AuthService"]
      contains: "signUp|signIn|signOut"
    - path: "lib/features/auth/screens/login_screen.dart"
      provides: "Login form with email + password"
      contains: "signInWithPassword"
    - path: "lib/features/auth/screens/signup_screen.dart"
      provides: "Signup form with email + password + display name"
      contains: "signUp"
    - path: "lib/features/auth/screens/onboarding_screen.dart"
      provides: "Multi-step wizard: household name → invite partner → done"
      contains: "Stepper|OnboardingNotifier"
    - path: "lib/features/auth/screens/invite_accept_screen.dart"
      provides: "Screen for second user to accept invite"
      contains: "accept_invite"
    - path: "lib/routing/app_router.dart"
      provides: "Full GoRouter with auth guards, onboarding redirect"
      contains: "redirect.*isLoggedIn|onboarding"
    - path: "lib/features/dashboard/screens/dashboard_screen.dart"
      provides: "Minimal dashboard showing wallet names as proof of successful auth + data flow"
      contains: "Scaffold"
  key_links:
    - from: "lib/routing/app_router.dart"
      to: "lib/services/auth/auth_service.dart"
      via: "GoRouter redirect checks auth state"
      pattern: "currentSession|isExpired"
    - from: "lib/features/auth/screens/onboarding_screen.dart"
      to: "Supabase households/wallets/wallet_categories tables"
      via: "Supabase client inserts during onboarding"
      pattern: "supabase.from.*households.*insert"
    - from: "lib/features/auth/screens/invite_accept_screen.dart"
      to: "public.accept_invite RPC"
      via: "Supabase .rpc('accept_invite')"
      pattern: "rpc.*accept_invite"
    - from: "lib/main.dart"
      to: "lib/services/auth/auth_service.dart"
      via: "Supabase.initialize provokes onAuthStateChange"
      pattern: "onAuthStateChange"
---

<objective>
Implement the complete auth flow (signup, login, logout, session persistence), household creation with invite system, onboarding wizard, and GoRouter auth guards. Wire everything end-to-end so a user can sign up, create a household, invite a partner, and land on a dashboard.

Purpose: Delivers AUTH-01 through AUTH-04 (auth requirements) and WALL-01 (auto wallet creation during onboarding/invite acceptance). This is the first user-facing functionality — after this plan, the app is usable for account creation and household setup.
Output: AuthService, 4 auth/onboarding screens, full GoRouter with auth redirects, wallet providers, placeholder dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md (§6 Auth Flow, §7 Onboarding Wizard, GoRouter auth guard)
@.planning/phases/01-foundation-schema/01-CONTEXT.md (locked: magic link invite, 7-day expiry, guided onboarding, 3 fixed wallets, preset categories, household user-named)
@.planning/phases/01-foundation-schema/01-VALIDATION.md
@.planning/phases/01-foundation-schema/01-01-SUMMARY.md (Flutter project structure, GoRouter stub, providers, main.dart)
@.planning/phases/01-foundation-schema/01-02-SUMMARY.md (Supabase schema, accept_invite RPC, handle_new_user trigger, RLS)
@.planning/phases/01-foundation-schema/01-03-SUMMARY.md (Drift AppDatabase, DAOs, database provider)
</context>

<interfaces>
<!-- Key contracts from prior plans that this plan depends on -->

From Plan 01 — lib/routing/app_router.dart (stub):
```dart
// GoRouter with placeholder routes: /auth/login, /auth/signup, /onboarding, /invite/:token, /
// Has redirect stub checking Supabase auth state
```

From Plan 01 — lib/main.dart:
```dart
// Supabase.initialize() with .env vars, ProviderScope wrapping app
```

From Plan 01 — lib/core/constants/app_constants.dart:
```dart
static const int inviteExpiryDays = 7;
static const List<String> defaultCategories = ['Food', 'Transport', ...];
```

From Plan 02 — Supabase RPC:
```sql
-- public.accept_invite(p_invite_token UUID) RETURNS UUID
-- Validates invite, adds member, creates private wallet, seeds categories
```

From Plan 02 — Supabase tables:
```sql
-- households: id, name, created_by
-- household_members: household_id, user_id, role
-- household_invites: household_id, invited_by, invite_code, invite_token, expires_at
-- wallets: id, household_id, user_id, wallet_type, name
-- wallet_categories: wallet_id, name, sort_order, is_income
-- profiles: id, display_name, household_id, onboarding_completed
```

From Plan 03 — lib/providers/database_providers.dart:
```dart
final appDatabaseProvider = Provider<AppDatabase>((ref) => AppDatabase());
```

From Plan 03 — lib/data/database/daos/:
```dart
// WalletDao: watchVisibleWallets(walletIds), createWallet(), getWalletsByHousehold()
// CategoryDao: seedDefaultCategories(walletId), watchCategories(walletId)
// HouseholdDao: createHousehold(), addMember(), getActiveMembers()
```
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Supabase project and link CLI</name>
  <action>
    This task requires the user to create a Supabase project and provide credentials.
  </action>
  <instructions>
    1. Go to https://supabase.com/dashboard and create a new project (free tier)
    2. Wait for the project to be provisioned (~2 minutes)
    3. Go to Settings → API and copy:
       - **Project URL** (e.g., https://xxxxx.supabase.co)
       - **anon/public key** (starts with eyJ...)
    4. Note the **project ref** from the dashboard URL (https://supabase.com/dashboard/project/**this-part**)
    5. Provide all three values when prompted

    After user provides values, Claude will:
    - Create `.env` file with SUPABASE_URL and SUPABASE_ANON_KEY
    - Run `npx supabase login` (user provides access token)
    - Run `npx supabase link --project-ref <ref>`
    - Run `npx supabase db push` to apply all migrations to remote
    - Run `npx supabase test db` to verify all pgTAP tests pass against remote
  </instructions>
  <resume-signal>Provide: SUPABASE_URL, SUPABASE_ANON_KEY, and project ref</resume-signal>
</task>

<task type="auto">
  <name>Task 2: AuthService + providers + login/signup screens</name>
  <files>lib/services/auth/auth_service.dart, lib/providers/auth_providers.dart, lib/features/auth/screens/login_screen.dart, lib/features/auth/screens/signup_screen.dart, lib/features/auth/widgets/auth_form_field.dart</files>
  <action>
    1. Create `lib/services/auth/auth_service.dart`:
       - Class `AuthService` that wraps Supabase Auth operations
       - Constructor takes `SupabaseClient` (for testability)
       - `Future<AuthResponse> signUp({required String email, required String password, String? displayName})` — calls `supabase.auth.signUp(email:, password:, data: {'display_name': displayName})` per research §6
       - `Future<AuthResponse> signIn({required String email, required String password})` — calls `supabase.auth.signInWithPassword(email:, password:)`
       - `Future<void> signOut()` — calls `supabase.auth.signOut()`
       - `User? get currentUser` — returns `supabase.auth.currentUser`
       - `Session? get currentSession` — returns `supabase.auth.currentSession`
       - `Stream<AuthState> get onAuthStateChange` — returns `supabase.auth.onAuthStateChange`
       - `bool get isAuthenticated` — session != null && !session.isExpired
       - `Future<Map<String, dynamic>?> getProfile()` — queries profiles table for current user
       - `Future<bool> get hasCompletedOnboarding` — checks profile.onboarding_completed

    2. Create `lib/providers/auth_providers.dart`:
       - `authServiceProvider` — Provider<AuthService> using Supabase.instance.client
       - `authStateProvider` — StreamProvider<AuthState> from authService.onAuthStateChange
       - `currentUserProvider` — Provider<User?> from authService.currentUser
       - `isAuthenticatedProvider` — Provider<bool>
       - `profileProvider` — FutureProvider that fetches current user's profile

    3. Create `lib/features/auth/widgets/auth_form_field.dart`:
       - Reusable `AuthFormField` widget — TextFormField with consistent styling, label, validator, obscureText option

    4. Create `lib/features/auth/screens/login_screen.dart`:
       - Form with email + password fields using AuthFormField
       - "Log in" button that calls authService.signIn
       - Link to signup screen ("Don't have an account? Sign up")
       - Loading state during auth call
       - Error display via SnackBar on failure
       - On success: GoRouter redirects to dashboard (handled by router redirect, not manual navigation)
       - Use ConsumerStatefulWidget with form key validation

    5. Create `lib/features/auth/screens/signup_screen.dart`:
       - Form with display name + email + password + confirm password
       - "Sign up" button that calls authService.signUp
       - Link to login screen ("Already have an account? Log in")
       - Password validation: minimum 8 chars
       - Loading state + error display
       - On success: router redirect to onboarding (for first users) or dashboard (for invite acceptors)
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos lib/services/ lib/providers/ lib/features/auth/</automated>
  </verify>
  <done>AuthService wraps all Supabase auth operations. Riverpod providers expose auth state. Login + signup screens render with forms, validation, loading states, and error handling. Auth calls would succeed against a live Supabase project.</done>
</task>

<task type="auto">
  <name>Task 3: Onboarding wizard, invite accept, dashboard, and GoRouter wiring</name>
  <files>lib/features/auth/screens/onboarding_screen.dart, lib/features/auth/screens/invite_accept_screen.dart, lib/features/dashboard/screens/dashboard_screen.dart, lib/providers/wallet_providers.dart, lib/routing/app_router.dart</files>
  <action>
    1. Create `lib/providers/wallet_providers.dart`:
       - `householdWalletsProvider` — FutureProvider that fetches wallets for user's household from Supabase
       - `visibleWalletIdsProvider` — Provider<List<String>> derived from wallets the user can see (shared + own private)

    2. Create `lib/features/auth/screens/onboarding_screen.dart` — per research §7:
       - Multi-step wizard using Flutter's `Stepper` widget (or PageView)
       - **Step 1: "Name your household"** — TextFormField for household name (e.g., "The Patels"). Validate non-empty, 2-50 chars.
       - **Step 2: "Invite your partner"** — Shows generated invite link + invite code. "Copy link" button + "Share" button (using share_plus or clipboard). "Skip for now" option.
       - **Step 3: "All set!"** — Confirmation with household name, wallets created summary. "Go to Dashboard" button.

       On Step 1 completion (per research §7 OnboardingNotifier pattern):
       a. Insert into `households` table (name, created_by = current user id)
       b. Insert into `household_members` (household_id, user_id, role='owner')
       c. Create shared wallet: insert into `wallets` (household_id, wallet_type='shared', name='{householdName} Shared')
       d. Create private wallet: insert into `wallets` (household_id, user_id=current user, wallet_type='private', name='My Wallet')
       e. Seed 8 default categories for BOTH wallets (from AppConstants.defaultCategories)
       f. Update `profiles` set household_id and onboarding_completed=true

       On Step 2 "Invite":
       a. Generate 8-char alphanumeric invite_code
       b. Insert into `household_invites` (household_id, invited_by, invite_code, expires_at = now + 7 days)
       c. Build magic link URL: `{appBaseUrl}/invite/{invite_token}`
       d. Display link + code to user

       Use a Riverpod Notifier to manage onboarding state (currentStep, householdName, isLoading, error). Show loading indicators during Supabase operations.

    3. Create `lib/features/auth/screens/invite_accept_screen.dart`:
       - Takes `token` from route parameter
       - On load: shows "Joining household..." with loading indicator
       - Calls Supabase RPC: `supabase.rpc('accept_invite', params: {'p_invite_token': token})`
       - On success: navigates to dashboard. RPC already creates wallet + categories.
       - On error: shows friendly message ("Invite expired", "Invalid invite", etc.)
       - If user is not authenticated, redirect to signup first (GoRouter handles this)

    4. Create `lib/features/dashboard/screens/dashboard_screen.dart`:
       - Minimal but functional dashboard:
         - AppBar with household name and logout icon button
         - Body shows list of user's visible wallets (shared + private) with their names
         - Each wallet shows its name and type badge ("Shared" / "Private")
         - Logout button calls authService.signOut()
       - Uses ConsumerWidget with householdWalletsProvider
       - This is a PLACEHOLDER dashboard — Phase 3 builds the real one

    5. Update `lib/routing/app_router.dart` (replace Plan 01 stub with full implementation):
       - GoRouter with `refreshListenable` tied to auth state changes (use GoRouterRefreshStream with supabase.auth.onAuthStateChange)
       - Routes:
         - `/auth/login` → LoginScreen
         - `/auth/signup` → SignupScreen
         - `/onboarding` → OnboardingScreen
         - `/invite/:token` → InviteAcceptScreen
         - `/` → DashboardScreen (home)
       - Redirect logic:
         a. If not authenticated AND not on /auth/* → redirect to /auth/login
         b. If authenticated AND on /auth/* → redirect to / (or /onboarding if not completed)
         c. If authenticated AND onboarding NOT completed AND not on /onboarding → redirect to /onboarding
         d. If authenticated AND onboarding completed AND on /onboarding → redirect to /
         - Check onboarding status by reading profile from Supabase (cache in provider)
       - Use GoRouterRefreshStream pattern per research §7

    Note: The redirect needs to check profile.onboarding_completed. This requires an async call. Use a combination of providers: load profile eagerly after auth state change, cache in provider, and check synchronously in redirect.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos && flutter build apk --debug 2>&1 | tail -5 && flutter build web 2>&1 | tail -5</automated>
  </verify>
  <done>Complete auth flow works end-to-end: signup → onboarding (create household + wallets + categories + invite) → dashboard showing wallets. Login → dashboard. Logout → login redirect. Invite accept → join household → dashboard. Flutter builds for both Android (APK) and Web without errors.</done>
</task>

</tasks>

<verification>
1. `flutter analyze --no-fatal-infos` — zero errors
2. `flutter build apk --debug` — Android build succeeds
3. `flutter build web` — Web build succeeds
4. Auth flow: signup creates user + profile, login returns session, logout clears session
5. Onboarding: creates household + 2 wallets + 16 categories (8 per wallet) + invite
6. Invite accept: calls RPC, auto-creates wallet + categories for second user
7. GoRouter: unauthenticated → /auth/login, authenticated without onboarding → /onboarding, authenticated with onboarding → /
</verification>

<success_criteria>
- User can sign up, log in, log out (AUTH-01, AUTH-02, AUTH-03)
- First user can create household and generate invite link (AUTH-04)
- Second user can accept invite and auto-get private wallet (AUTH-04, WALL-01)
- Session persists across app restarts (AUTH-02)
- Flutter app builds APK + Web from single codebase (PLAT-05)
- GoRouter correctly redirects based on auth state and onboarding completion
- Dashboard shows wallet names proving end-to-end data flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-04-SUMMARY.md`
</output>
